"""
src — top-level package for PhysGTO.

Sub-packages:
    src.model   — neural-operator architectures
    src.dataset — dataset classes

Utility modules (unchanged location):
    src.train, src.utils
"""
